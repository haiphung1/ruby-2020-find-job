<div class="featured-wrap loginWrp">
  <div class="container"> 
    <% if @posts.any? %>
      <ul class="listService">
        <%= render @posts %>
      </ul>
      <%= paginate @posts %>
    <% else %>
      <h2><%= t "cv.applied_post" %></h2>
    <% end %>
  </div>
</div>
