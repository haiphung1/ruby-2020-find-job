<div class="inner-content loginWrp resumeWrp">
  <div class="container"> 
      <div class="row">
        <div class="col-md-9 col-sm-8">
          <% if @profile.any? %>
            <ul class="listService">
              <%= render @profile %>
            </ul>
            <%= paginate @profile %>
          <% end %>
        </div>
      </div>
    </div>
</div>
